<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:itmit.asb.app.Controls"
             xmlns:app="clr-namespace:itmit.asb.app"
             mc:Ignorable="d"
             NavigationPage.HasNavigationBar="False"
             BackgroundImageSource="mainf.png"
             x:Class="itmit.asb.app.Views.ForgetPasswordPage">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Ваш номер:" FontSize="Large" TextColor="White" Margin="30,30,30,0"/>
            <controls:LoginEntry x:Name="Login" Placeholder="ТЕЛЕФОН" Text="{Binding Login}"
                                         Style="{StaticResource EntryLoginPageNumber}">
                <controls:LoginEntry.Behaviors>
                    <app:MaskedBehavior Mask="+X (XXX) XXX-XX-XX" />
                </controls:LoginEntry.Behaviors>
            </controls:LoginEntry>
            <Button Text="Отправить" Style="{StaticResource ButtonLoginPage}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>