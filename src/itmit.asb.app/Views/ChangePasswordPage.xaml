<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:itmit.asb.app.Controls"
             x:Class="itmit.asb.app.Views.ChangePasswordPage"
             NavigationPage.HasNavigationBar="False"
             BackgroundImageSource="mainf.png">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Введите новый пароль" FontSize="Large" TextColor="#f4e213" Margin="30,30,30,0"/>
            <controls:LoginEntry Text="{Binding ChangePassword}" Placeholder="Новый пароль" Margin="30,0" Style="{StaticResource EntryLoginPageNumber}"/>
            <controls:LoginEntry Text="{Binding ChangePasswordСonfirmation}" Placeholder="Подтверждение пароля" Margin="30,0" Style="{StaticResource EntryLoginPageNumber}"/>
            <Button Text="Отправить" Style="{StaticResource ButtonLoginPage}" Command="{Binding VerifyPassword}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>